# Configuration of the camera
camera :
{
    resolution = { width = 900; height = 900; };
    position = { x = 0; y = 0; z = 100 ; };
    rotation = { x = 0; y = 0; z = 0; };
    fieldOfView = 120.0;
};

# Primitives in the scene
primitives :
{
    # List of spheres
    spheres = (
        { x = 0; y = 0; z = 0; r = 80; color = { r = 255; g = 64; b = 64; }; },
    );

};

# Light configuration
lights :
{
    ambient = 0.4;
    diffuse = 0.6;
    point = (
        { x = 400; y = 100; z = 500; }
    );
    directional = ();
};
